package shapes;
// TODO: Inside of the shapes directory, create a Measurable interface with the following methods:
public interface Measurable {
//    both method signatures not implementations dont need a modifier because they inherit it from the interface.
    double getPerimeter();
    double getArea();
}
